name: charter-live-1789258707
on: workflow_dispatch
jobs:
  pass:
    runs-on: ubuntu-latest
    steps:
      - run: echo "the live check ran this"
      - run: mkdir -p out && echo '{"ok": true}' > out/report.json
      - uses: actions/upload-artifact@v4
        with:
          name: charter-live-1789258707-report
          path: out/
  fail:
    runs-on: ubuntu-latest
    steps:
      - run: echo "about to fail on purpose"
      - run: |
          echo "::error::the live check made this job fail"
          exit 1
